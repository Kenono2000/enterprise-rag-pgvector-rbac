# enterprise-rag-pgvector-rbac
Production-grade, asynchronous RAG reference implementation. Features class-based singleton client initialization, fail-fast configuration validation, in-database JSONB RBAC filtering, and Pydantic-enforced deterministic output contracts with citation tracking.
